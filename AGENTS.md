# Stock Lab 开发约束

本项目是独立的个人选股、回测和模拟盘实验平台，不连接真实券商，不执行真实交易。

## 长期规则

1. 禁止自动切换 Git 分支；切换、创建分支或 worktree 前必须先询问用户。
2. 提交信息、发布说明和 GitHub 描述使用中文。
3. 构建或发布时，源码和 `frontend/dist/` 必须一起提交并推送 GitHub，远程推送失败不得继续发布。
4. `frontend/dist/` 不得加入 `.gitignore`。
5. 数据库、环境变量、API Key、缓存和日志不得提交。
6. 行情源、选股规则、回测引擎彼此解耦；不得让大模型直接完成收益或资金计算。
7. 回测只能使用对应交易日当时已经存在的数据，避免未来数据泄漏。
8. 当前项目只用于学习和研究，任何页面都必须保留“不构成投资建议”的提示。

## 目录

- `backend/`：FastAPI、行情数据、条件选股、Skill、策略和回测引擎。
- `frontend/`：React 管理界面，生产基础路径 `/stock/`。
- `deploy/`：后续服务器配置和安全发布脚本。
- 运行时数据库默认位于 `data/stock_lab.db`，不得提交。

